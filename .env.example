# Cloudflare API Configuration
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Account.Cloudflare Tunnel:Edit, Zone.DNS:Edit
CLOUDFLARE_API_TOKEN=your_api_token_here

# Your Cloudflare Account ID
# Find it at: https://dash.cloudflare.com/ (right sidebar)
CLOUDFLARE_ACCOUNT_ID=your_account_id_here

# Default domain for container hostnames
# Example: if set to "example.com", container "web" becomes "web.example.com"
CLOUDFLARE_DEFAULT_DOMAIN=example.com

# Tunnel name (will be created if it doesn't exist)
# Default: "default-tunnel"
CLOUDFLARE_TUNNEL_NAME=my-docker-tunnel

# Optional: Zone ID (auto-detected from domain if not provided)
# CLOUDFLARE_ZONE_ID=your_zone_id_here

# Cloudflare Tunnel Token (required for cloudflared container)
# Get this from: https://one.dash.cloudflare.com/ → Networks → Tunnels → [your-tunnel] → Configure
# Or run: cloudflared tunnel token [tunnel-name]
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here
